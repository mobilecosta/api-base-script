# ═══════════════════════════════════════════════════════════════
# Authentication API - Production Environment Configuration
# ═══════════════════════════════════════════════════════════════
# 
# This file contains the environment variables needed for Vercel deployment.
# 
# ⚠️  IMPORTANT: Do NOT commit this file to git if it contains actual values!
# Set these variables in Vercel's dashboard instead:
# Project Settings → Environment Variables
#
# Guide:
# https://vercel.com/docs/concepts/projects/environment-variables
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
# Application Environment
# ───────────────────────────────────────────────────────────────

# Node environment
NODE_ENV=production

# ───────────────────────────────────────────────────────────────
# Supabase Configuration
# See: https://supabase.com/docs
# ───────────────────────────────────────────────────────────────

# Your Supabase project URL
# Find at: https://app.supabase.com/project/[project-id]/settings/api
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (public, safe to expose)
# Find at: https://app.supabase.com/project/[project-id]/settings/api
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key (⚠️  SECRET - keep private!)
# Find at: https://app.supabase.com/project/[project-id]/settings/api
# Only for backend use. Set this in Vercel's secret variables.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ───────────────────────────────────────────────────────────────
# JWT Configuration
# ───────────────────────────────────────────────────────────────

# Secret key for signing JWT tokens
# Generate: openssl rand -base64 32
# ⚠️  VERY IMPORTANT: Keep this secret and long (32+ characters)
JWT_SECRET=your-very-long-random-secret-key-minimum-32-characters

# Token expiration time
# Format: 7d, 24h, 3600s, etc.
JWT_EXPIRES_IN=7d

# ───────────────────────────────────────────────────────────────
# Database Configuration (Optional - if using external DB)
# ───────────────────────────────────────────────────────────────

# Connection string for external database (if not using Supabase)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# ───────────────────────────────────────────────────────────────
# Logging Configuration
# ───────────────────────────────────────────────────────────────

# Log level: debug | info | warn | error
LOG_LEVEL=info

# ───────────────────────────────────────────────────────────────
# CORS Configuration
# ───────────────────────────────────────────────────────────────

# Allowed origins for CORS (comma-separated)
# Should include your frontend domain
CORS_ORIGINS=https://frontend-domain.com,https://app.frontend-domain.com

# ═══════════════════════════════════════════════════════════════
# How to set these in Vercel:
# ═══════════════════════════════════════════════════════════════
# 
# 1. Go to https://vercel.com/dashboard
# 2. Select your project
# 3. Click "Settings"
# 4. Go to "Environment Variables"
# 5. Add each variable:
#    - Name: Variable name (e.g., SUPABASE_URL)
#    - Value: The value (e.g., https://your-project.supabase.co)
#    - Select environments: Production (check the box)
# 6. Click "Save"
# 7. Trigger a new deployment for changes to take effect
#
# ═══════════════════════════════════════════════════════════════
# Getting Supabase Credentials:
# ═══════════════════════════════════════════════════════════════
#
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Click "Settings" (bottom left)
# 4. Click "API"
# 5. Copy the values:
#    - Project URL → SUPABASE_URL
#    - anon/public key → SUPABASE_ANON_KEY
#    - service_role key → SUPABASE_SERVICE_ROLE_KEY
#
# ═══════════════════════════════════════════════════════════════
# Generating JWT_SECRET:
# ═══════════════════════════════════════════════════════════════
#
# Linux/Mac:
#   openssl rand -base64 32
#
# Windows PowerShell:
#   [Convert]::ToBase64String((1..32 | ForEach-Object { [byte](Get-Random -Max 256) }))
#
# Online tool (not recommended for production):
#   https://www.random.org/bytes/
#
# ═══════════════════════════════════════════════════════════════
